"use client";import {useEffect,useState} from "react";import {useRouter,useSearchParams} from "next/navigation";export default function Callback(){const router=useRouter();const params=useSearchParams();const[message,setMessage]=useState("Google लॉगिन पूरा हो रहा है…");useEffect(()=>{const code=params.get("code");if(!code){setMessage("लॉगिन कोड नहीं मिला");return}fetch("/api/backend/auth/exchange",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({code})}).then(async r=>{if(!r.ok)throw new Error((await r.json()).detail);router.replace("/admin");router.refresh()}).catch(e=>setMessage(e.message||"लॉगिन विफल"))},[params,router]);return <main className="loginPage"><section className="loginCard"><h1>{message}</h1></section></main>}
