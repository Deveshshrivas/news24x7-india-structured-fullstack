"use client";import {FormEvent,useState} from "react";import {useSearchParams} from "next/navigation";
export default function LoginPage(){const search=useSearchParams();const[mode,setMode]=useState<"login"|"register">("login");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch(`/api/backend/auth/${mode}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const d=await r.json();setBusy(false);if(!r.ok){setError(d.detail||"लॉगिन नहीं हुआ");return}window.location.assign(search.get("next")||"/admin")}return <main className="loginPage"><section className="loginCard"><a className="loginBrand" href="/"><b>NEWS<span>24×7</span></b><small>INDIA</small></a><h1>{mode==="login"?"एडमिन लॉगिन":"नया खाता बनाएँ"}</h1><p>न्यूज़रूम और ऑडियो डैशबोर्ड में सुरक्षित प्रवेश</p><a className="googleLogin" href="/api/backend/auth/google">G&nbsp;&nbsp; Google से जारी रखें</a><div className="loginOr"><span/>या ईमेल से<span/></div><form onSubmit={submit}>{mode==="register"&&<label>नाम<input required name="name" minLength={2}/></label>}<label>ईमेल<input required name="email" type="email"/></label><label>पासवर्ड<input required name="password" type="password" minLength={8}/></label>{error&&<div className="loginError">{error}</div>}<button className="primary" disabled={busy}>{busy?"कृपया रुकें…":mode==="login"?"लॉगिन करें":"खाता बनाएँ"}</button></form><button className="loginSwitch" onClick={()=>setMode(mode==="login"?"register":"login")}>{mode==="login"?"पहली बार आए हैं? खाता बनाएँ":"पहले से खाता है? लॉगिन करें"}</button></section></main>}
