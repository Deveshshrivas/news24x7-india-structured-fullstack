import {NextResponse} from "next/server";export async function GET(request:Request){const r=NextResponse.redirect(new URL("/",process.env.NEXT_PUBLIC_SITE_URL||request.url));r.cookies.delete("news_token");return r}
